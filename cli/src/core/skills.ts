// Auto-generated from ../../skills/*.md — run "npm run build:skills" to update.
// Do not edit manually.

export interface SkillDef {
  id: string;
  description: string;
  argHint: string;
  content: string;
}

export const SKILLS: SkillDef[] = [
  {
    id: 'taskpact',
    description: '智能入口：读状态自动决定下一步',
    argHint: '[需求或\'状态\']',
    content: `# taskpact: 智能入口

一个命令搞定整条流水线。你读取当前状态，自动判断该做什么，然后转入对应子流程。

用户不需要记住 init / clarify / implement / verify 的顺序——这是你的工作，不是用户的。

## 输入

\`\`\`
/taskpact                      # 看状态，自动决定下一步
/taskpact 我要做一个用户注册功能   # 带需求 → 通常进澄清
/taskpact 继续                  # 继续当前合约
/taskpact 状态                  # 只看进度，不动手
\`\`\`

## 核心：先判断状态，再行动

**第一步永远是搞清楚现在在哪。** 不要凭对话记忆，一切以磁盘上的文件为准。

\`\`\`
1. .spec/config.md 不存在？
   → 项目还没初始化。
   → 如果 .spec/ 目录也不存在：提示用户先运行 \`npx taskpact init\`（CLI 装技能文件），再回来。
   → 如果 .spec/ 存在但 config.md 缺失或全是占位注释：转入 taskpact-init 流程（扫描项目、填 config.md）。

2. config.md 存在且有真实内容？
   → 扫描 .spec/contracts/*.md，读每个合约的 frontmatter status。
   → 按下面的"状态 → 动作"表决定下一步。
\`\`\`

## 状态 → 动作

扫描所有合约的 status，**唯一真相源是合约 frontmatter 的 status，不是 config.md，也不是对话历史。**

| 现状 | 你的动作 |
|------|---------|
| 用户带了**新需求**描述 | 转入 **taskpact-clarify**，把需求作为输入。即使已有其他合约，新需求优先建新合约。 |
| 没有任何合约 + 无需求 | 问用户想做什么，然后转入 **taskpact-clarify**。 |
| 有 \`draft\` 合约 | 澄清没走完。转入 **taskpact-clarify** 继续把它补全到 \`agreed\`。 |
| 有 \`agreed\` 合约 | 合约已锁定，等实现。转入 **taskpact-implement**。 |
| 有 \`in_progress\` 合约 | 实现中或有 fail 待修。转入 **taskpact-implement**（它会自己判断是首次实现还是修复模式）。 |
| 有 \`needs-review\` 合约 | **停下来交给人。** 读验证记录，把需人工确认的点（[必须] uncertain、超轮次、反复 fail）清楚列给用户，等指示。不要自作主张置 done。 |
| 只有 \`done\` 合约 | 实现已自验证通过。问用户：要不要跑一轮独立 **taskpact-verify** 交叉验证？或者开始下一个需求？ |
| 多个在途合约（agreed/in_progress） | 列出来让用户选，或按用户指定的合约名继续。 |

## 怎么"转入"子流程

确定动作后：

1. **先用一句话告诉用户你的判断和决定**，例如：
   > 当前有 1 个 \`agreed\` 合约 \`user-register\`，还没实现。我来执行实现流程。
2. 然后**调用对应的子技能**（taskpact-clarify / taskpact-implement / taskpact-verify / taskpact-init / taskpact-status），完整遵循它的指令。
3. 子流程跑完后，回到这里复盘：状态变了吗？要不要自动衔接下一步（比如 implement 完成 → 提示可以 verify）？

**衔接原则：** 能自动往下走的就往下走，但**两个地方必须停下来问人**——
- 进入 clarify 的追问环节（需要用户回答澄清问题）。
- 合约进入 \`needs-review\`（需要人工判断）。
其余环节（agreed→implement→自验证）你应当自主完成，这正是"用户去喝茶"的含义。

## 只看状态（用户问"状态/进度/到哪了"）

转入 **taskpact-status** 流程：渲染所有合约的进度表，不修改任何文件。

## 注意事项

- **永远先读盘再动手。** config.md + 合约文件 + git log 是你的外部记忆，对话历史不是。
- **不要重复用户已经做过的决定。** 如果合约已 agreed，别再问需求；直接实现。
- **状态判断错了，整条流水线就错了。** 拿不准当前该做什么时，宁可用一句话跟用户确认，也不要猜。
- 这个入口是为了**省去用户记命令**，不是为了替用户做产品决策。澄清阶段的业务选择仍然要问用户。
`,
  },
  {
    id: 'taskpact-init',
    description: '扫描项目、生成 .spec/config.md',
    argHint: '[项目描述]',
    content: `# taskpact: 项目初始化

AI 引导式项目初始化。扫描项目，生成 \`.spec/config.md\`，帮用户快速进入开发状态。

> 这一步和 CLI 的 \`npx taskpact init\` **不是一回事**：CLI 负责把技能文件铺进你的工具（一次性），本技能负责**扫描你的项目、填好 config.md**（每个项目一次）。先跑 CLI，再跑这个。

## 输入

用户调用 \`/taskpact-init\`，可选带一句话描述：

\`\`\`
/taskpact-init
/taskpact-init 我要做一个 Todo API
/taskpact-init 帮我初始化一个 React + Express 项目
\`\`\`

---

## 前置检查

\`\`\`
1. 检查 .spec/ 目录是否存在
   - 不存在 → 提示用户先运行 \`npx taskpact init\` 安装技能文件，然后再回来
   - 存在 → 继续

2. 检查 .spec/config.md 内容
   - 不存在，或是骨架（全是 <!-- 占位注释 -->、无实际内容）→ 视为未初始化，继续扫描填充
   - 已有实际内容 → 展示当前配置，问用户是否要重新扫描覆盖（保留用户手填的项目原则）
\`\`\`

---

## 流程

### Step 1: 理解意图

根据用户的输入判断场景：

**场景 A：已有项目，初始化 taskpact**
用户在一个已有代码的项目里运行，没有特别的需求描述。
→ 扫描项目，生成 config.md，完成。

**场景 B：已有项目 + 有具体需求**
用户在已有项目里运行，并带了需求描述（如"我要加一个用户注册功能"）。
→ 扫描项目，生成 config.md，然后自动衔接到 \`/taskpact-clarify\` 流程。

**场景 C：空项目 + 有需求描述**
用户在空目录运行，并描述了想做什么（如"帮我初始化一个 React + Express 项目"）。
→ 搭建最小可运行脚手架，生成 config.md，然后询问用户是否要开始第一个合约。

**判断规则：**
\`\`\`
目录下有源代码文件（.ts/.py/.go/.java 等）？
  有 → 已有项目（场景 A 或 B）
  无 → 空项目（场景 C）

用户带了需求描述？
  有 → 场景 B 或 C
  无 → 场景 A
\`\`\`

### Step 2: 扫描项目

**已有项目（场景 A/B）：**

扫描并收集以下信息：

\`\`\`
1. 项目标志文件 → 推断技术栈
   package.json → Node.js (读 dependencies 进一步判断框架)
   go.mod → Go
   Cargo.toml → Rust
   pom.xml → Java (Maven)
   build.gradle → Java (Gradle)
   pyproject.toml / setup.py → Python
   ...

2. 目录结构 → 理解代码组织
   src/、tests/、lib/ 等目录的存在和布局

3. 已有测试 → 推断测试框架和命名风格
   grep -r "describe\\|it(\\|test(" tests/ 或 src/
   查看 test runner 配置（jest.config、vitest.config、pytest.ini 等）

4. 构建配置 → 推断构建命令
   package.json scripts → npm run build / npm test
   Makefile → make build / make test
   Dockerfile → 了解部署方式

5. 已有 CI 配置 → 了解项目约束
   .github/workflows/ → 看跑了什么检查
\`\`\`

**空项目（场景 C）：**

根据用户描述的需求，决定技术栈并搭建脚手架：

\`\`\`
1. 追问关键信息（如果描述不够明确）：
   - 用什么语言/框架？
   - 前端、后端、还是全栈？

2. 搭建项目结构
   - 创建目录结构（src/、tests/ 等）
   - 生成项目配置文件（package.json、tsconfig.json 等）
   - 安装基础依赖
   - 创建入口文件
   - 创建基础测试配置

3. 确保项目能跑
   - 运行构建命令验证
   - 运行测试命令验证（即使没有测试用例，框架要能跑通）
\`\`\`

### Step 3: 收集项目原则

**在生成 config.md 之前，主动询问用户整个开发过程中必须遵循的规则。**

这些原则对所有合约生效：clarify 会参考它做推荐，implement 和 verify 会在结尾**逐条核对**每条原则、把核对结果写进合约的验证记录。所以原则写得越可核对越好——能机械校验的（如 lint、覆盖率），尽量写清判断标准。

\`\`\`
问: 有没有整个项目必须遵循的开发原则？比如代码规范、架构约束、质量标准、安全策略等。
推荐: 基于项目扫描结果给出推荐（例如从 .eslintrc 推断代码规范，从已有测试推断测试策略）。
\`\`\`

用户可以直接接受推荐，也可以补充、修改。如果用户没有特别的原则，保持为空即可。

### Step 4: 生成 config.md

基于扫描结果和用户确认的项目原则，生成完整的 \`.spec/config.md\`。

**config.md 只存静态项目配置——不存"当前状态/阶段"。** 现在该做什么由各合约的 status 推导，config.md 不重复记录，避免两套状态漂移。

\`\`\`markdown
# 项目配置

## 上下文
Node.js + TypeScript + Express REST API。
使用 Vitest 做测试，pnpm 管理依赖。

## 操作命令
- 构建: npm run build
- 测试: npm test
- 单测: npx vitest run -t "<test_name>"
- 启动: npm run dev

## 项目原则
- 所有 API 返回 JSON，错误用 { code, message } 格式
- TypeScript strict 模式，禁止 any
- 测试覆盖率 ≥ 80%
\`\`\`

**展示给用户确认。** 用户可以修改。确认后写入文件。

### Step 5: 创建目录结构

\`\`\`
.spec/
├── config.md       ← 刚生成的
└── contracts/      ← 空目录，合约会放这里
\`\`\`

### Step 6: 衔接下一步

**场景 A（纯初始化）：**
\`\`\`
taskpact 初始化完成！

已创建:
  ✓ .spec/config.md
  ✓ .spec/contracts/

下一步: /taskpact <你的需求>   （会自动开始澄清）
       或直接 /taskpact-clarify <你的需求>
\`\`\`

**场景 B（已有项目 + 有需求）：**
\`\`\`
taskpact 初始化完成！现在帮你开始第一个合约。
\`\`\`
然后直接进入 clarify 流程，把用户的需求描述作为输入。

**场景 C（空项目 + 有需求）：**
\`\`\`
项目脚手架搭建完成！

已创建:
  ✓ 项目结构（src/、tests/、package.json 等）
  ✓ .spec/config.md
  ✓ .spec/contracts/

要不要为你的第一个功能创建合约？（/taskpact-clarify）
\`\`\`
用户同意后进入 clarify 流程。

---

## 注意事项

- **不要瞎猜技术栈。** 看文件、读配置、查依赖。推断不出来就问用户。
- **config.md 的操作命令必须能跑。** 如果你写了 \`npm test\`，先确认 package.json 里有 test script。
- **单测命令格式很重要。** 这是 implement 和 verify 阶段跑单个场景测试用的，必须准确（含 \`<test_name>\` 占位符）。
- **脚手架要能构建通过。** 搭完就跑一遍构建和测试，确保基础是稳的。
- **不要过度初始化。** 用户要的是能工作的起点，不是完美的项目模板。该简洁就简洁。
- **重新初始化时保留用户手填的项目原则**，不要无脑覆盖用户的配置。
`,
  },
  {
    id: 'taskpact-clarify',
    description: '把模糊需求转化为精准的合约',
    argHint: '<需求描述>',
    content: `# taskpact: 合约澄清

帮用户把模糊需求转化为精准的 Task Contract。产出一个符合 taskpact 格式的合约文件。

## 你的角色

你是一个需求分析师。你通过逐条追问，把用户脑子里的模糊想法变成 AI agent 可以精确执行、机器可以精确验证的合约。

你不讨论"怎么实现"——那是实现阶段的事。你只关注"做什么"和"怎么算做完"。
唯一的例外：当"做什么"取决于技术选择时（比如 WebSocket vs SSE 影响功能特性），你可以讨论技术选择。

## 输入

用户调用时会带一句话描述，例如：
\`\`\`
/taskpact-clarify 我要做一个用户注册功能
/taskpact-clarify 修复退款接口超时的问题
/taskpact-clarify 给订单列表加分页
\`\`\`

这句话就是你的起点。可能很模糊，可能很清晰。无论如何，你先理解大方向，再逐步追问细节。

## 工作流程

### Step 1: 理解初始需求 + 加载项目上下文

1. 读用户的初始描述，理解大方向（做什么类型的事？新功能？修 bug？重构？）
2. 检查是否存在 \`.spec/config.md\`，如果有则读取（技术栈、操作命令、项目原则）
3. 快速扫描项目结构，了解技术栈和代码组织方式
4. 如果是修 bug 或改已有功能，先定位相关代码，了解现状
5. 这些信息帮你在后续追问中给出更准确的推荐答案

### Step 2: 逐条追问

**一次只问一个问题。每个问题附带你的推荐答案。** 问完这一个就停下，等用户回答，再问下一个。

用户可以直接接受推荐，也可以修改。这样用户负担最小。

> **硬规则（不可违反，越自信的模型越容易破戒）：**
> - **一条消息只做一件事**：要么问一个问题、要么生成合约——**绝不在同一条消息里既问问题、又替用户答、还把合约一起甩出来**。
> - **追问没走完之前，禁止跳到 Step 3 生成完整合约。** 哪怕你"觉得"已经想清楚了——你想清楚的是你脑补的版本，不是和用户对齐的版本。
> - 如果你发现自己正准备一次性输出一整份合约、而用户还没回答过几个问题，**立刻停下，回到一次一问。**

**查代码优先原则：**
在问用户之前，先查代码库。能从代码得到的答案不要问用户：
- 现有 API 的 URL 格式、返回结构 → \`grep -r "router\\|@GetMapping\\|app.get" src/\`
- 现有错误码和错误处理模式 → 查代码中的 error handling
- 现有测试的命名和组织方式 → \`ls tests/\` 或 \`find . -name "*test*"\`
- 现有 UI 的组件库和设计模式 → 查 package.json 或 import 语句

**追问顺序：**

\`\`\`
1. 意图：做什么？为什么做？（1-2 个问题就够）
2. 场景枚举 + 优先级：列出主要场景，确定每个的优先级（必须/应该/可选）
3. 逐场景深入：对每个场景，逐一过维度清单（见下方）
4. 约束与决策：能改什么？不能动什么？已确定的技术选择？
\`\`\`

**场景优先级：**
- **必须** — 没有就不能交付，MVP 的一部分
- **应该** — 重要但不阻塞交付，可以第二轮补
- **可选** — 有更好，没有也行

**第 3 步——维度清单：**

每个场景都过一遍以下维度。根据需求类型自行判断哪些适用，不适用的跳过：

| 维度 | 要澄清的问题 | 适用场景 |
|------|-------------|---------|
| 数据流 | 输入什么？输出什么？状态怎么变？ | 所有 |
| 交互 | 用户怎么触发？看到什么？布局、配色、动效？ | 有 UI |
| 异常 | 失败了怎么办？边界值？并发？幂等？ | 所有 |
| 可验证性 | 怎么测？测试数据从哪来？用什么框架？ | 所有 |
| 性能 | 数据量多大？响应时间要求？ | 有性能敏感场景 |
| 安全 | 鉴权？权限？输入校验？ | 有用户输入或权限 |

这不是一个需要逐条问的列表。这是一个检查清单——你在深入每个场景时，自然地覆盖相关维度。
一个场景可能只涉及 2-3 个维度，另一个可能涉及 5 个。按场景的性质判断。

**停止追问的标准：**
对每个场景，你自己做以下检查。全部通过才能停：
- 能写出明确的 假设/当/那么 步骤
- 有明确的验证方式（测试函数名 或 代码审查 或 其他）
- 你自己能判断这个场景是 pass 还是 fail
- **这个场景的关键决策来自用户的回答，不是你单方面的假设**
如果你判断不了，或某个 [必须] 场景全靠你脑补、用户从没确认过——说明还不够清楚，**继续追问，不许进 Step 3。**

**例外 · 开放式设计任务**（如架构规格、技术选型探索）：逐条追问有时不如先给一版草案再迭代。这时**允许**直接出草案，但**必须明说一句**："这是开放设计，我直接给草案供你改，而不是逐条问。" —— 透明地偏离可以，偷偷地跳过不行。

**推荐答案示例：**
\`\`\`
问: 注册时如果邮箱已存在，返回什么状态码？
推荐: 409 Conflict。我查了你现有的 login API（src/api/auth/login.ts:23），
     重复场景也是用 409。保持一致比较好。
\`\`\`

### Step 3: 生成合约

追问结束后，生成完整的合约文件。格式严格遵循 Task Contract：

\`\`\`markdown
---
spec: task
name: "简短名称"
status: draft
round: 0
max_rounds: 3
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

## 意图

2-3 句话说清楚做什么、为什么做。

## 已定决策

- 已确定的技术选择 1
- 已确定的技术选择 2

## 边界

### 允许修改
- path/to/files/**

### 禁止做
- 不要修改 xxx
- 不要引入 xxx

## 完成条件

场景: 场景名称 [必须]
  测试: test_function_name
  假设 前置条件
  当 用户操作
  那么 期望结果
  并且 其他期望

场景: 另一个场景 [应该]
  测试: test_another_function
  假设 ...
  当 ...
  那么 ...

场景: 锦上添花的场景 [可选]
  测试: test_optional_feature
  假设 ...
  当 ...
  那么 ...
\`\`\`

**优先级含义：**
- \`[必须]\` — implement 必须完成，不做不能交付
- \`[应该]\` — implement 尽量完成，但不阻塞交付
- \`[可选]\` — implement 有余力再做

**完成条件的写法规则：**

后端/逻辑场景——绑定测试函数：
\`\`\`
场景: 注册成功
  测试: test_register_returns_201
  假设 不存在邮箱为 "alice@example.com" 的用户
  当 客户端 POST /api/v1/users/register，body 为 {email, password}
  那么 响应状态码为 201
  并且 响应体包含字段 "user_id"
\`\`\`

前端/UI 场景——标记验证方式：
\`\`\`
场景: 注册页面布局
  验证方式: 代码审查
  假设 用户访问 /register 页面
  那么 页面居中显示注册表单，max-width: 400px
  并且 主按钮使用 Ant Design Primary Button
  并且 按钮文本为"注册"
\`\`\`

**测试函数命名规则：**
- 从现有测试中推断命名风格（查 tests/ 目录）
- 如果项目没有测试，使用 \`test_<模块>_<行为>\` 格式
- 测试名要能看出测什么：\`test_register_rejects_duplicate_email\` 好，\`test_register_2\` 差
- **测试此刻还不存在是正常的**——implement 阶段会按这个名字创建测试。合约里的名字就是契约，implement 必须用一模一样的函数名，不许改名。

### Step 4: 用户确认

1. 展示完整合约给用户审核，并**显式标出哪些决策是用户明确确认过的、哪些是你推荐但用户还没拍板的**——让用户一眼看出你替他假设了什么
2. 用户可能要求修改——修改后重新展示
3. 用户确认后：
   - 创建 \`.spec/contracts/\` 目录（如不存在）
   - 文件名用 kebab-case：\`.spec/contracts/<name>.md\`（如 \`user-register.md\`、\`fix-login-timeout.md\`）
   - 写入合约文件，**frontmatter \`status\` 设为 \`agreed\`**（这是该合约进入"等待实现"的唯一标志，不需要在 config.md 另记状态）
   - 告诉用户合约已锁定，下一步可以 \`/taskpact\`（自动实现）或 \`/taskpact-implement\`

## 注意事项

- **完成条件是合约的核心**。意图和决策可以简短，但完成条件的每个场景必须精确到可验证。
- **不要替用户做业务决定**。你可以推荐，但最终选择权在用户。技术决定（用什么状态码、用什么格式）你可以基于代码库直接推荐。
- **场景宁多不少**。漏掉的场景 = 漏掉的验证 = 可能的 bug。至少覆盖：成功路径、主要失败路径、边界情况。
- **每个场景必须独立**。不依赖其他场景的执行顺序。
- **主观标准必须客观化**。"好看" 不是验收标准。需要具体化、客观化。
- **最常见的失败：不追问、直接交卷。** 用户说一句需求、你回一句"好的"，然后甩出一整份合约——这不是 clarify，是替用户拍板。clarify 的价值就在那几轮一问一答里，跳过它，整个方法论就废了一半。
`,
  },
  {
    id: 'taskpact-implement',
    description: '按合约逐条实现功能',
    argHint: '[合约名]',
    content: `# taskpact: 自主实现

按合约中的完成条件逐条实现功能。

用户已经离开。你需要自主完成所有工作。

## 核心原则

**合约文件（不是对话历史）是 source of truth。**

**铁律：没有通过测试的场景，不算完成。**

四重约束——动手写代码之前必须读完：
1. **项目原则** — config.md 中的全局规则，所有合约都必须遵守
2. **已定决策** — 怎么做（已确定的技术选择，不需要你再做判断）
3. **边界** — 碰什么（允许修改的路径 + 禁止修改的路径）
4. **完成条件** — 什么时候算完（BDD 场景，每个场景有优先级和验证方式）

## 输入

用户调用 \`/taskpact-implement\`，可选指定合约名。

### 合约选择

\`\`\`
1. 如果用户指定了合约名 → 直接使用
2. 扫描 .spec/contracts/ 下所有合约，筛选 status 为 agreed 或 in_progress 的
3. 只有一个候选 → 自动选中
4. 有多个候选 → 列出合约名和 status，让用户选
5. 没有候选 → 告诉用户 "没有可实现的合约，请先运行 /taskpact-clarify"
\`\`\`

**选中后必须报告：** "使用合约: xxx (status: agreed)"

---

## 启动

\`\`\`
1. 找到目标合约文件，读取全部内容
2. 读取 .spec/config.md → 获取构建命令、测试命令、单测命令、项目原则
3. 检查 status:
   - agreed       → 首次实现，把合约 frontmatter 的 status 更新为 in_progress
   - in_progress  → 继续实现（可能有验证记录，见"修复模式"）
   - needs-review → 之前判过需人工介入。除非用户明确要你继续，否则停下来说明情况
   - 其他         → 停止，状态不对
4. 列出所有场景，按优先级和依赖关系确定实现顺序:
   - [必须] 场景优先，全部完成才算可交付
   - [应该] 场景其次，尽量完成
   - [可选] 场景最后，有余力再做
\`\`\`

**状态只写在合约 frontmatter 里。** config.md 不记"当前阶段"——避免两套状态漂移。合约的 status 就是唯一真相。

**必须在写第一行代码之前完成启动。**

---

## 逐场景实现

按优先级顺序逐个实现。每个场景走完整个流程再进入下一个。

**单个场景的完整流程：**

\`\`\`
1. 读取场景的 假设/当/那么 描述，理解清楚要做什么
2. 检查 已定决策 和 边界 → 确认实现方式和可修改的文件范围
3. 写代码
   - 严格遵守边界中的路径限制
   - 严格使用已定决策中的技术选择
   - 遵守 config.md 中的项目原则
4. 处理测试
   - 场景有 \`测试:\` 字段 → 确保该测试函数存在，函数名与合约**完全一致**，覆盖场景描述的全部断言
   - 场景没有 \`测试:\` 字段且非代码审查 → 创建测试，将函数名回填到合约
5. 用 config.md 的单测命令运行该场景的测试 → 必须通过
6. 运行构建命令 → 必须通过
7. git add + git commit（提交信息: [taskpact] 实现场景: <场景名>）
\`\`\`

**不要跳步。不要批量实现多个场景再一起测试。一个场景一个提交。**

---

## 修复模式

如果合约已有 \`## 验证记录\` 且包含 fail 的场景，说明之前有人验证过并写回了结果。此时只修复 fail 的场景：

\`\`\`
1. 读取验证记录，找到所有 fail 场景和失败原因
2. 失败原因格式：期望什么、实际什么、问题在哪个文件哪一行
3. 根据失败原因直接定位问题并修复
4. 不动 pass 场景的代码（除非修复 fail 必须改）
5. 修复后运行该场景的测试 → 必须通过
6. 运行全量测试 → 确认没有回归
7. git commit（提交信息: [taskpact] 修复场景: <场景名>）
\`\`\`

**关键规则：不修改完成条件的场景描述和验收标准。元数据（测试函数名、阻塞原因、验证记录）可以写入。**

---

## 自验证

所有场景实现/修复完成后，你必须自己跑一轮完整验证。**你交付的必须是能通过验证的结果。**

\`\`\`
1. 运行构建命令 → 必须通过
2. 运行全量测试 → 必须通过
3. 逐场景确认：
   - 有 \`测试:\` 字段 → 用单测命令运行，确认通过
   - 有 \`验证方式: 代码审查\` → 读代码确认符合场景描述
   - [应该/可选] 场景未实现 → 标记 skip
   - 记录每个场景的结果（pass / fail / skip / uncertain）
4. 以用户身份实际运行产出物，确认核心功能可用
   - 这不是建议，是必须执行的步骤
   - 运行产出物的真实入口（启动服务、执行命令、打开页面等）
   - 验证 [必须] 场景对应的功能确实能用
   - 在验证记录中写明你运行了什么、结果是什么
5. 逐条核对项目原则（config.md 的「## 项目原则」）
   - 把每一条原则单独拎出来，对照本次改动逐条判定：满足 / 违反 / 不适用 / uncertain
   - 违反的当场修复，不能只在记录里"记一笔"就放过
   - 判不准是否满足的标 uncertain，按第 7 步 status 规则处理
6. 写入验证记录（来源: 自验证），包含两部分：
   - 场景结果：逐场景 pass / fail / skip / uncertain
   - 原则核对：逐条列出每条项目原则及判定（满足 / 违反 / 不适用 / uncertain）
7. 判定 status（写入合约 frontmatter）:
   - 所有 [必须] 场景 pass，没有 [必须] 场景是 fail 或 uncertain，且没有项目原则被违反或 uncertain → status: done
   - 有 [必须] 场景 fail，或有项目原则被违反 → 回到修复模式，修完再跑一轮自验证（status 保持 in_progress）
   - 有 [必须] 场景 uncertain，或有项目原则 uncertain（你无法确定是否满足）→ status: needs-review，验证记录里清楚写明哪个场景/哪条原则、为什么无法确定。【不要置 done】
   - 非必须场景的 uncertain/skip → 不阻塞 done，但要在验证记录里列出
   - round 已达 max_rounds 仍有 [必须] fail → status: needs-review，说明已尽最大轮次
8. git commit（提交信息: [taskpact] 实现完成: <合约名>  或  [taskpact] 需人工介入: <合约名>）
\`\`\`

**为什么 uncertain 的 [必须] 场景不能置 done：** done 是"绿灯可信"的承诺。如果一个必须场景你都拿不准是否满足，置 done 等于把不确定性藏起来骗用户。诚实地转 needs-review，让人来拍板。

用户后续运行 \`/taskpact-verify\` 是独立的交叉验证。

---

## 遇到阻塞

如果某个场景无法实现（依赖缺失、需求矛盾、测试始终无法通过）：

\`\`\`
1. 在合约该场景下方写一行: 阻塞原因: <具体原因>
2. git commit（提交信息: [taskpact] 阻塞场景: <场景名>）
3. 跳过，继续下一个场景
4. 如果被阻塞的是 [必须] 场景，自验证时该合约判 needs-review
\`\`\`

---

## 上下文管理

你的上下文窗口有限。合约文件在磁盘上，不受上下文压缩影响。

**合约文件是你的外部记忆。随时保持它是最新的。**

### 恢复协议

如果你感觉上下文被压缩了，或者用户 clear 后重新调用了 implement，立即执行：

\`\`\`
1. 读取 config.md → 构建/测试/单测命令 + 项目原则
2. 扫描 .spec/contracts/ → 找 status 为 in_progress / agreed 的活跃合约
3. 读取合约文件 → 获取场景列表、当前 status、验证记录
4. 查看 git log → 找 [taskpact] 开头的提交，知道自己做到哪了
5. 对比场景列表和已提交的场景 → 确定下一个未完成的场景
6. 继续实现
\`\`\`

**不要依赖对话记忆。一切以 config.md、合约文件和代码为准。**

---

## 常见错误

| 你可能犯的错误 | 正确做法 |
|--------------|---------|
| 没读完合约就开始写代码 | 启动步骤必须完整执行 |
| 修改了禁止修改的文件 | 每次改文件前检查边界 |
| 场景没有通过测试就提交了 | 铁律：必须有通过的测试 |
| 只跑了单测没跑构建 | 每个场景：测试 + 构建都通过 |
| 上下文压缩后凭记忆继续 | 立即执行恢复协议 |
| 修复 fail 时改了 pass 的代码 | 最小化修复，改完跑全量测试 |
| 凭直觉判断 pass/fail | 必须通过运行测试或代码审查来验证 |
| 一次实现多个场景再提交 | 一个场景一个提交 |
| 测试函数名和合约不一致 | 合约写什么名就用什么名 |
| 偏离合约，加了没要求的功能 | 只做合约要求的，多余的不做 |
| 修复时降低合约标准 | 修代码，不改场景描述和验收标准 |
| 实现完就交差，没跑自验证 | 必须跑完自验证再交付 |
| [必须] 场景拿不准却置 done | 转 needs-review，让人来判 |
| 项目原则没逐条核对就置 done | 自验证里逐条核对每条原则，违反必修、结果写进验证记录 |
`,
  },
  {
    id: 'taskpact-verify',
    description: '逐条验证合约完成条件',
    argHint: '[合约名]',
    content: `# taskpact: 交叉验证

独立验证合约完成条件。implement 已经自验证过一轮——你是独立的第二双眼睛。

**你只改合约文件。绝不修改源代码、测试代码或任何其他文件。**

## 核心原则

- **独立判断。** 不依赖 implement 的验证记录，从零开始重新验证每个场景。
- **诚实判定。** fail 就是 fail，uncertain 就是 uncertain。不要美化。
- **失败原因必须可操作。** 让修复的人能直接根据你写的原因定位问题，不需要再调查。

## 输入

用户调用 \`/taskpact-verify\`，可选指定合约名。

### 合约选择

\`\`\`
1. 如果用户指定了合约名 → 直接使用
2. 扫描 .spec/contracts/ 下所有合约，筛选 status 为 done 或 in_progress 或 needs-review 的
3. 只有一个候选 → 自动选中
4. 有多个候选 → 列出合约名和 status，让用户选
5. 没有候选 → 告诉用户 "没有可验证的合约"
\`\`\`

**选中后必须报告：** "验证合约: xxx (status: done, round: N)"

---

## 启动

\`\`\`
1. 找到目标合约文件，读取全部内容
2. 读取 .spec/config.md → 获取构建命令、测试命令、单测命令、项目原则
3. 检查 status:
   - done          → 正常，交叉验证 implement 的成果
   - in_progress   → 可以验证（可能是手动触发或修复轮）
   - needs-review  → 可以验证，帮人确认那些不确定项
   - 其他          → 停止，状态不对
4. 检查 max_rounds:
   - round >= max_rounds → 仍可跑这一轮验证，但如果验证后还有 [必须] fail，
                           status 判为 needs-review 并提示用户: "已达最大验证轮次 (N)，需人工介入"
\`\`\`

**状态只写在合约 frontmatter 里。** config.md 不记"当前阶段"。

---

## 先跑构建

用 config.md 中的构建命令编译项目。如果构建失败：

\`\`\`
- 所有场景直接标记 fail
- 失败原因写 "构建失败" + 错误信息摘要
- 跳到"更新合约"，不用逐条验证
\`\`\`

---

## 逐条验证

对合约完成条件中的每个场景，根据验证方式判定结果。

**优先级处理规则：**
- \`[必须]\` 场景：正常验证
- \`[应该]\` 场景：正常验证，但测试不存在且无实现痕迹 → \`skip\`
- \`[可选]\` 场景：测试不存在且无实现痕迹 → \`skip\`
- \`skip\` 表示"未实现"，不等于 fail，不阻塞 done

### 有 \`测试:\` 字段

用 config.md 中的**单测命令**运行指定测试（将 \`<test_name>\` 替换为场景的测试函数名）。

\`\`\`
测试通过   → pass
测试失败   → fail，记录测试输出中的错误信息
测试不存在 → [必须] 场景: fail，记录 "测试函数未找到: <函数名>"
             [应该/可选] 场景: skip，记录 "未实现"
\`\`\`

### 有 \`验证方式: 代码审查\`

读代码检查是否符合场景描述。

\`\`\`
能明确判定符合   → pass
能明确判定不符合 → fail，写明哪里不符合
无法确定         → uncertain，写明原因
\`\`\`

### 都没有

尝试从场景描述推断验证方法。如果实在无法验证 → uncertain。

---

## 核对项目原则

逐条验证完场景后，对照 config.md 的「## 项目原则」做独立核对——你是第二双眼睛，从当前代码和产物重新判断，不采信 implement 的结论。

对每一条项目原则：

\`\`\`
满足   → 记 满足
违反   → 记 违反 + 哪个文件哪一行违反了哪条原则（你不改代码，写清楚让 implement 去修）
不适用 → 记 不适用（本次改动不涉及这条）
判不准 → 记 uncertain + 原因
\`\`\`

**项目原则违反 = [必须] 场景 fail；项目原则 uncertain = [必须] uncertain。** 一并代入下面的 status 判定。

---

## 更新合约

验证完成后，更新合约文件中的两个部分：

### 1. 验证记录

如果合约已有 \`## 验证记录\`，**替换它**（不追加）。如果没有，在末尾添加。

只保留当前这一轮的结果：

\`\`\`markdown
## 验证记录

来源: 交叉验证
轮次: N
时间: YYYY-MM-DD HH:MM

- 场景名称: pass
- 场景名称: fail — 具体原因（期望 X，实际 Y。问题在 文件:行号）
- 场景名称: skip — [可选] 场景未实现
- 场景名称: uncertain — 原因（无法自动验证 UI 渲染效果）

原则核对:
- 原则一: 满足
- 原则二: 违反 — src/api/x.ts:45 用了 any，违反"TypeScript strict，禁止 any"
- 原则三: 不适用

结论: X pass, Y fail, Z skip, W uncertain
需人工确认: 列出 uncertain 的场景名，以及违反/uncertain 的项目原则
\`\`\`

### 2. Frontmatter

**更新 status（写入合约 frontmatter，这是唯一真相源）。** 把"项目原则违反"计入 [必须] fail、"项目原则 uncertain"计入 [必须] uncertain，再按下表判定：
\`\`\`
所有 [必须] 场景 pass，且无 [必须] fail/uncertain（skip 不阻塞）     → status: done
有 [必须] 场景 fail，且 round < max_rounds                          → status: in_progress（通知 implement 修复）
有 [必须] 场景 fail，且 round >= max_rounds                         → status: needs-review（人工介入）
有 [必须] 场景 uncertain（无法判定是否满足）                          → status: needs-review（人工确认）
\`\`\`

**更新 round：** round 值 +1

**更新 updated：** 今天日期

不需要再去改 config.md——现在该做什么由合约 status 推导，verify 把 status 写对就够了。

---

## 失败原因的写法

失败原因必须包含三要素，让修复的人能直接定位：

\`\`\`
差: "注册功能有问题"
差: "测试失败了"

好: "期望返回 409，实际返回 500。
    src/api/auth/register.ts:45 的 catch 块没有处理 DuplicateEmailError，
    直接抛出了 InternalServerError。"
\`\`\`

**三要素：期望什么、实际什么、问题在哪。**

---

## 恢复

verify 每次从零开始验证全部场景，无需增量恢复。
如果被中断（clear 或上下文压缩），直接重新运行 \`/taskpact-verify\`。
扫描 .spec/contracts/ 找到活跃合约即可开始。

---

## 常见错误

| 你可能犯的错误 | 正确做法 |
|--------------|---------|
| 修改了源代码或测试代码 | 只改合约文件 |
| 没跑构建就开始逐条验证 | 先跑构建，失败则全部 fail |
| 测试没跑就标 pass | 有测试绑定的场景必须用单测命令实际运行 |
| 失败原因写得模糊 | 三要素：期望、实际、位置 |
| 基于实现过程的记忆判断 | 只看当前代码和测试结果 |
| 美化结果，把 fail 写成 uncertain | 能判定的就判定，uncertain 留给真正无法确定的 |
| 把 [必须] uncertain 当 done | [必须] uncertain → needs-review，不置 done |
| 验证记录追加而不是替换 | 只保留当前轮次，覆盖之前的 |
| [可选] 未实现标为 fail | 未实现的 [应该/可选] 标 skip，不是 fail |
| 只验场景，没核对项目原则 | 项目原则逐条核对，违反按 [必须] fail 处理 |
`,
  },
  {
    id: 'taskpact-status',
    description: '只读：渲染所有合约的进度表',
    argHint: '[合约名]',
    content: `# taskpact: 状态总览

回答"喝完茶回来，进展到哪了？"。**只读不写**——不修改任何文件。

## 输入

\`\`\`
/taskpact-status
/taskpact-status user-register   # 只看某个合约
\`\`\`

## 数据来源

唯一真相源是磁盘上的合约文件，不是对话历史。

\`\`\`
1. 读 .spec/config.md → 项目上下文、操作命令（可选，用于展示）
2. 扫描 .spec/contracts/*.md → 每个合约的 frontmatter（status/round/max_rounds）
   + ## 完成条件里的场景列表和优先级
   + ## 验证记录里每个场景最近一轮的 pass/fail/skip/uncertain
\`\`\`

> 如果项目装了 CLI，\`npx taskpact status\` 会用代码确定性地解析出同样的表格，不依赖 AI 判断。本技能是它的对话版。

## 输出：进度表

为每个合约渲染一张表，按 status 分组（在途的排前面）：

\`\`\`
合约: user-register        status: in_progress   轮次: 1/3
  完成条件:
    ✓ 注册成功            [必须] pass
    ✗ 重复邮箱被拒绝       [必须] fail — 期望 409，实际 500（register.ts:45）
    ? 注册页面布局        [应该] uncertain — 无法确认渲染
    – OAuth 登录          [可选] skip
  小结: 1 pass · 1 fail · 1 uncertain · 1 skip
  下一步: 有 [必须] fail → /taskpact-implement 进入修复模式
\`\`\`

图例：✓ pass · ✗ fail · ? uncertain · – skip · ○ 未验证

## 每个 status 对应的"下一步"建议

| status | 含义 | 建议下一步 |
|--------|------|-----------|
| \`draft\` | 澄清未完成 | \`/taskpact-clarify\` 补全到 agreed |
| \`agreed\` | 已锁定待实现 | \`/taskpact-implement\`（或直接 \`/taskpact\`） |
| \`in_progress\` | 实现中 / 有 fail 待修 | \`/taskpact-implement\`（首次或修复模式） |
| \`needs-review\` | 需人工介入 | 看验证记录里"需人工确认"的项，人来拍板 |
| \`done\` | [必须] 全 pass | 可选 \`/taskpact-verify\` 做独立交叉验证 |

## 注意事项

- **绝不修改文件。** 这是只读命令。
- 如果某合约缺 \`## 验证记录\`，场景标"○ 未验证"，不要瞎猜结果。
- 如果 status 与验证记录看起来矛盾（比如 status: done 但记录里有 [必须] fail），**如实指出这个不一致**，提示用户跑一轮 verify 校正——这正是状态可信度的价值。
`,
  },
];
